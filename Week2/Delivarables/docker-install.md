# Week 2 Deliverable
Docker was installed and verified on the Ubuntu VM.

[Docker Notes](../Docker/Notes.md)

The Docker installation was verified using:

```
docker --version
sudo systemctl status docker
docker info
