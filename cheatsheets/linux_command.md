
| Command |  What it does
| ------- | -------------------------
| `pwd`   | Where am I?
| `ls`    | What's here? Lists files and folders in the current diectory. 
| `mkdir` | Create a folder 
| `cd`    | Move between folders 
| `cd ..` | Go up one level 
| `cd ~`  | Go home 
| `touch` | Create a file 
| `nano`  | Edit a file 
| `cat`   | Display a file's contents 
| `cp`    | Copy and duplcate a file 
| `top`   | Show the current traffic
| `rm`    | Remove a file 
| `sudo`  | Runs command as admin
| `grep`  | Search text for a match
| `tree`  | Shows folders in a tree structure
| `cat`   | Displays the content of a file in the terminal
| `whoami`| Displays the username of the currently logged in user
| `mv`    | moves or renmes file and folders
| `echo`  | prints texts into a file. >> append to the content, not overwrite it. 
| `head`  | show the beginning of a file
| `tail`  | show the end of a file
| `rmdir` | Deletes an empty directory
| `rm -r` | Deletes a directory and everythin inside it
| `man`   |  Opens detailed documentation 
| `--help`| Shows quick command help 
| `which` | Shows where a command is installed 
| `find`  | Searches for files/folders 
| `grep`  | Searches inside files
| `apt`   | Installs and manages software
